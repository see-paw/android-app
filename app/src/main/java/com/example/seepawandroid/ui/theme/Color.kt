package com.example.seepawandroid.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Light theme primary color (80% tint).
 */
val Purple80 = Color(0xFFD0BCFF)

/**
 * Light theme secondary color (80% tint).
 */
val PurpleGrey80 = Color(0xFFCCC2DC)

/**
 * Light theme tertiary color (80% tint).
 */
val Pink80 = Color(0xFFEFB8C8)

/**
 * Dark theme primary color (40% tint).
 */
val Purple40 = Color(0xFF6650a4)

/**
 * Dark theme secondary color (40% tint).
 */
val PurpleGrey40 = Color(0xFF625b71)

/**
 * Dark theme tertiary color (40% tint).
 */
val Pink40 = Color(0xFF7D5260)

/**
 * Background color for available time slots in the schedule.
 */
val AvailableSlotColor = Color(0xFFE8F5E9)

/**
 * Background color for reserved time slots in the schedule.
 */
val ReservedSlotColor = Color(0xFFFFECB3)

/**
 * Background color for unavailable time slots in the schedule.
 */
val UnavailableSlotColor = Color(0xFFFFEBEE)

/**
 * Background color for empty time slots in the schedule.
 */
val EmptySlotColor = Color(0xFFF5F5F5)

/**
 * Background color for time slots reserved by the current user.
 */
val OwnReservationColor = Color(0xFFE3F2FD)
